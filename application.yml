spring:
  config:
    use-legacy-processing: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
      
---
spring:
  profiles: prod

eureka:
  client:
    service-url:
      defaultZone: http://eureka:8761/eureka/
